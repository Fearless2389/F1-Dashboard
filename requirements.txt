# ── Existing ML / data stack ─────────────────────────────────────────────────
fastf1>=3.3.0
pandas>=2.0.0
numpy>=1.26.0
scipy>=1.12.0
pyarrow>=14.0.0
scikit-learn>=1.4.0
xgboost>=2.0.0
lightgbm>=4.3.0
shap>=0.44.0
matplotlib>=3.8.0
tqdm>=4.66.0

# ── FastAPI backend ──────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
websockets>=12.0
pydantic>=2.6.0
python-multipart>=0.0.9

# ── Live data layer ──────────────────────────────────────────────────────────
httpx>=0.27.0
cachetools>=5.3.0
apscheduler>=3.10.0

# ── Streamlit (legacy — kept until React parity confirmed) ───────────────────
streamlit>=1.32.0
